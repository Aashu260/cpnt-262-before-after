// Check if Integer: You can use a method to determine if a value is a whole number (an integer).

// Before
// Declaring the variables and prinitng their values
let num1 = 10.5;
let num2 = 21;
console.log("1st value: " + num1);
console.log("2nd value: " + num2);
// After
// Checking the values if the number is interger or not
console.log("Is the first number an Integer? " + Number.isInteger(num1));
console.log("Is the second number an Integer? " + Number.isInteger(num2));
